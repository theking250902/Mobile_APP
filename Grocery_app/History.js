import { View, Text } from 'react-native'
import React from 'react'

const History = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center',}}>
      <Text style={{fontSize: 18, color: '#000', fontWeight: 'bold'}}>History</Text>
    </View>
  )
}

export default History